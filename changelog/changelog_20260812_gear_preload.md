# Gear preload and scrub restoration — 2026-08-12

- Restored the original 1.1-second GSAP scrub smoothing.
- Added an initial HTML preload hint for `sci_fi_gear_animated.glb` so the 49 MB model starts downloading before the application bundle executes.
